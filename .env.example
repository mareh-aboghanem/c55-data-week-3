# The weather pipeline uses only public APIs and local files.
# No secrets are required to run it.
#
# If you add your own data sources (e.g. a paid weather API),
# add the keys here and load them with python-dotenv:
#
#   import os
#   from dotenv import load_dotenv
#   load_dotenv()
#   API_KEY = os.getenv("MY_API_KEY")

# MY_API_KEY=your_key_here
